const config = {
  // Set to "true" to hide the Day of the Week filter button in the home page.
  hideDayOfTheWeekFilter: true,

  // Set to "true" to hide the Product Type filter button in the home page.
  hideProductTypeFilter: true,

  enableApiFlag: false,
};

export default config;
