const config = {
  // Set to "true" to showe the Day of the Week filter in the home page.
  showDayOfTheWeekFilter: false,

  // Set to "true" to showe the Product Type in the home page.
  showProductTypeFilter: false,
};

export default config;
